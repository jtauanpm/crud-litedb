pipeline {
    agent any

    stages {
        // Continuous Integration CI
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                script {
                    echo '!!!!!!!!!!build!!!!!!!!!!!'
                }
            }
        }
    }
}

